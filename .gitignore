# ── macOS ──
.DS_Store

# ── Xcode ──
build/
DerivedData/
*.xcuserstate
*.xcscmblueprint
xcuserdata/
*.moved-aside
*.pbxuser
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# ── Swift Package Manager ──
.build/
.swiftpm/
Package.resolved

# ── Xcode project ──
# StringTheory.xcodeproj and App/StringTheory/Info.plist are committed and opened
# directly in Xcode. project.yml is kept only as a reference for the original setup.
# Per-user Xcode state stays ignored (see xcuserdata above).

# ── Fastlane / tooling output ──
fastlane/report.xml
fastlane/Preview.html
fastlane/screenshots/**/*.png
fastlane/test_output
